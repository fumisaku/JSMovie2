<Window x:Class="カメラ選択"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="カメラ選択" Height="300" Width="350"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="使用するカメラを選択してください" FontSize="14" Margin="0,0,0,15"/>

        <RadioButton x:Name="RB_00" Grid.Row="1" Content="カメラ 0 (内蔵カメラ)" FontSize="13" Margin="0,5" IsChecked="True"/>
        <RadioButton x:Name="RB_01" Grid.Row="2" Content="カメラ 1 (USBカメラ)" FontSize="13" Margin="0,5"/>
        <RadioButton x:Name="RB_02" Grid.Row="3" Content="カメラ 2" FontSize="13" Margin="0,5"/>

        <Button Grid.Row="5" Content="設定" Click="PB_設定_Click" Height="35" FontSize="14" Margin="0,10,0,0"/>
    </Grid>
</Window>
